# Fresh experiment data

No historical recordings, processed data or fitted models are active. Add one folder per new preliminary take under raw_takes, preserving its original OptiTrack CSV and controller logger CSV together. The dataset manifest is empty until takes are imported/reviewed. See docs/NEW_SYSTEM_CHECK.md.
