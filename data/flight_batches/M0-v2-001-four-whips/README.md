# M0_v2_001: four individual whips

In Rehearsals > Recorded takes, click Refresh takes and select whip_001 through whip_004. Each contains five seconds of preceding hover and the recorded recovery. The fourth ends during the final hold.

For the original M0 prediction overlay: Flight comparison > Flights by model > Refresh > M0 > MPPI session with 4 takes. Select Take 001–004 and click Load real flight + ghost. Uncheck Whip only to include recovery.

Native data rows, coordinates and timestamps are preserved. Alignment is estimated, not synchronized. All four entries belong to one recording/battery. There is no physical target or cable intervention. Roles remain unassigned; this split does not fit a model or grant training eligibility.

Replay alignment was revised to +0.746 seconds using fresh controller-position updates. The prior +0.795-second estimate included stale cached samples. See synchronization_verification.json; the previous alignment is preserved in alignment_history. Segment boundaries were originally selected with the prior estimate and still cover more than five seconds before execution and the available recovery. OptiTrack supplies the native 100 Hz fitting targets; controller positions are only an estimated timing reference.
