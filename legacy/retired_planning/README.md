# Superseded planning source

This directory contains the Milestone-4 MPPI and pre-production CEM paths.
Production planning now uses `planning/production_cem.py` and the final
normalized-action/settle/replay contract. The GUI no longer launches these
runners; it only reads saved results.

The archived source remains available for scientific provenance but is not an
active runtime dependency.
