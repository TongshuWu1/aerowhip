# Auto detect text files and perform LF normalization
* text=auto
*.sh text eol=lf
*.cmd text eol=crlf

# OptiTrack evidence required to reproduce data processing and cable validation.
data/raw_takes/**/*.csv filter=lfs diff=lfs merge=lfs -text
data/processed_takes/**/*.npz filter=lfs diff=lfs merge=lfs -text

# Preserve immutable research snapshots, provenance hashes, and flown CSV bytes.
data/** -text
runs/** -text
policies/** -text
config/experiments/** -text
rehearsal_csv_and_result_in_real_flight/** -text
*.csv -text

# Research snapshot binaries and recorded/derived numerical data.
*.pt filter=lfs diff=lfs merge=lfs -text
*.npz filter=lfs diff=lfs merge=lfs -text
*.npy filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text
data/**/*.csv filter=lfs diff=lfs merge=lfs -text
runs/**/*.csv filter=lfs diff=lfs merge=lfs -text
rehearsal_csv_and_result_in_real_flight/**/*.csv filter=lfs diff=lfs merge=lfs -text

# Preserve exact exported evidence bytes at the canonical locations.
exports/** -text
paper/figures/** -text

# Preserve exact external manuscript snapshot bytes and provenance hashes.
paper/overleaf/** -text

# User-authorized runtime snapshot: keep exported CSVs in LFS and exact temp/output bytes.
exports/**/*.csv filter=lfs diff=lfs merge=lfs -text
output/** -text
tmp/** -text
workspace/** -text
experiments/** -text

# Exact-byte predecessor evidence still referenced by the current horizontal model.
archive/horizontal-renumber-20260916/earlier-attempt/runs/adaptation/M7-horizontal-20260915/** -text
