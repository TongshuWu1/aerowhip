# Retired simulator checkout

This prototype is preserved as a historical, detached checkout during the
AeroWhip consolidation to `main` and `deployment`. Its final commit is retained
in main's merge ancestry. The ignored synthetic runs remain in this directory.

The active research project is `../aerowhip` on `main`. The lab application is
on `deployment`. Do not resume this paused Isaac prototype automatically.

The 12 existing CPU math tests passed during consolidation on Windows.
No Isaac simulation, fitting, training or physical experiment was started.