# New M0 collection

The active experiment has been reset to the newest retained M0 and its unchanged slower-brake CSV. Previous M0/M1/M2 whipping recordings, fitted later models, corrections, and evaluations are archived; they are not part of this new collection.

Collect ten new takes and retain all raw OptiTrack/controller pairs under `exports/M0_Bspline_slower_brake_1s/flight_take/`. The user intends all ten for subsequent training. Evaluate frozen models before assimilating each new batch. Detailed perturbation choices and the fitting workflow change are not implemented by this artifact cleanup.

The original model, command, reference timing, hardware settings, and motion limits are unchanged. Preliminary M0 fitting data remain as dependencies. Original evidence and the previous protocol are recoverable through the reset archive.
