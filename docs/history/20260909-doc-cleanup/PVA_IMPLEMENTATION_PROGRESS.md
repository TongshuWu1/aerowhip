> Historical document archived on 9 September 2026. For current work, read [HANDOFF.md](../../../HANDOFF.md). Old running-job and launch instructions below are historical.

# Direct PVA workflow — authorized implementation

## Latest user scope: MPPI only, no fitting

Completed MPPI continuation:43 tests and six-page Windows UI audit pass.
Selected historical-M1 simulation hits within3.094cm; optimization141.73s,
27iterations. Final run20260909-115040-750483 uses its unchanged saved whip;
complete recovery/portable replay verified exactly. Read
`docs/history/20260909-doc-cleanup/MPPI_PVA_VERIFICATION_20260909.md`. No workers remain active.

9 September: user will investigate physical drone tracking first. Keep the
bootstrap fit, PPO campaign and heartbeat stopped. Continue direct-PVA MPPI
implementation and bounded end-to-end simulation checks using frozen historical
normalized M1. It is not the unfinished fresh M0 and does not validate the
current drone. Earlier required fit/PPO work below is suspended.

MPPI now evaluates the proposal mean as one extra candidate, excluded from
importance weights. Best-plan metrics refer to the exact retained trajectory;
atomic plan publication and explicit failed/hit status protect UI/export.
Rehearsal waits for a stopped/completed job and rejects incomplete recovery
prediction. The initial default-noise diagnostic was infeasible and correctly
blocked export. A frozen-model sampling sweep justified reducing latent noise
from0.5 to0.05; active MPPI temperature is1 instead of10. Launch, task, reward,
physics and constraints are unchanged. See the MPPI continuation audit.

## Latest state: stopped for performance work

9 September daytime: user stopped fitting/campaign to prioritize performance.
Fit stopped cleanly at full-whip update105 (selection loss0.1362765566), with
current/best weights, optimizer and plateau state retained. It has NOT reached
plateau. Campaign stopped before PPO launch; heartbeat PAUSED. No production
restart. The running/queued instructions below are historical.

Performance implementation and verification complete for the dominant active
paths: cable full-whip loss/gradients236.3→3.13s (~75×), drone residual0.249→
~0.12s (~2×), shared PPO/MPPI1024-trajectory rollout4.57→2.60s (~1.75×).
Same float64 physics, complete objective, rewards and commands.31 tests passed;
full gradient parity passed; saved smoke CSV regenerated byte-identically.
Read `docs/PVA_PERFORMANCE_20260909.md` for exact evidence and limits. Do not
restart the stopped fit/campaign automatically or overwrite retained state.

9 September 2026. User approved the discussed design and requested completion
of implementation, normalized-adp0 bootstrap fitting and a full UI overhaul.
Worktree: particle_filter_cable_project, branch twin-rewrite. PhysX is paused.

## Contract

- Both methods construct desired tracked-origin P/V/A from bounded XYZ jerk.
  Integrate exact constant-jerk segments; emit 30 Hz packets with kinematic
  acceleration and the existing heading contract. No virtual force planning.
- Evaluate the exact held packets through the existing loaded-drone pose
  response + drone residual + rotated attachment + DDER + cable residual.
- PPO and MPPI have independent settings/runs. Old force policies, exports and
  CSV-matched ghosts keep their historical interpretation. New PPO starts fresh.
- Fit preliminary M0 from five normalized cf7/adp0 takes only. Use explicit
  nominal estimates/geometry and zero-output fresh residuals, no old learned
  model prior. Record cf7 157 g + 18 g provenance separately from current cf3
  hardware 153 g. This is a retrospective local bootstrap, not a verified cf3
  model or physical-world normalized hitting result.
- Existing stored normalization/alignment is frozen and applied once. Do not
  refit these transforms or reuse old fitted M1. Future post-hover normalization
  remains retrospective. Preserve originals.
- Fits use practical plateau stopping and a safety ceiling, best weights and
  essential combined-whip checks, not automatic expensive leave-one-out refits.

## Work stages

1. IN PROGRESS: fresh cold bootstrap job `runs/adaptation/20260909-pva-M0-bootstrap`.
   Drone fit completed at plateau update880, mean per-take RMS about2.56cm.
   Physics least squares completed: EI5.900135620909374e-7,
   Cb3.241538013397505e-8; ftol stop, no active bounds.
   Slow physical autograd attempt was stopped; files retained. A short-window
   cable-NN attempt worsened whole-whip selection despite reducing short loss.
   It was stopped after update18 and is retained; best checkpoint was zero NN.
   ACTIVE continuation is FULL-WHIP cable NN (`--continue-cable-full-whip`),
   stdout/stderr `fullwhip.*.log`, history `cable/residual_full_whip/history.json`.
   Parent launch PID8996, actual worker15480 (check status.json). About4min per
   full-whip update expected. Do not restart or duplicate it.
2. IMPLEMENTED + core tests passed: jerk/PVA, fixed command-history observation,
   direct streaming pose/cable dynamics, exact delayed held packets, reward,
   per-row invalidity and terminal/export boundary behavior.
3. IMPLEMENTED: separate `planning/pva_job.py` PPO/MPPI workers and immutable
   jobs, source snapshots, checkpoint-family rejection, rehearsal/export.
   A16-attempt PPO smoke and two-iteration MPPI smoke completed; both produced
   full rehearsals. They are integration tests, NOT learned flight policies,
   and are marked ARCHIVED in their run folders (no files deleted).
   PPO smoke `20260909-033109-123778`, MPPI `20260909-034614-449845`.
   Rehearsals `runs/rehearsals_pva/20260909-pva-smoke` and `20260909-mppi-smoke`.
4. IMPLEMENTED: six-page PVA main window; independent planner controls/libraries,
   model fit monitoring, plots including episode points, shared saved rehearsal
   browser, preserved historical force tools and normalized exact-CSV flights.
   Actual Windows Qt/Fusion/VTK audit: `runs/audits/pva-ui`, six pages no errors.
   Native framebuffer `rehearsal-render.png` is valid; Qt grab shows black only
   for its native OpenGL child. No renderer fallback was introduced.
5. VERIFIED core/UI/export: final focused suite30 passed; subsequent7 UI/export
   checks passed after status/style changes. Existing protected config/policy/
   export/data hashes all unchanged. Portable MPPI ZIP extracted and regenerated
   with identical CSV SHA and exactly zero drone/cable difference:
   `runs/audits/pva-portable/result.json`. No Linux/hardware flight claim.
6. Final UI consistency pass: external campaign runs now appear automatically,
   library status updates preserve selection, model names use their saved labels,
   and orange deterministic evaluation curves show the actual PPO stopping
   signal. Fit logs include the active continuation logs. Six targeted UI tests
   passed after these changes. Explicit PVA continuation preserves optimizer
   moments while applying the new run's saved learning rate to both optimizers.

## Required remaining work

- Let full-whip NN fit finish or plateau. Inspect essential combined diagnostics
  and the reported train/executor agreement. Candidate publishes to this job's
  `candidate/model.json`; selectable only with provenance.fit_complete=true.
  If a failure occurs, inspect and fix it; never mark completion from a ceiling.
- Portable source/asset ZIP and current UI checks are complete. Recheck only
  if a later model/training/export failure identifies a new issue.
- ALREADY QUEUED: managed campaign `runs/pva_campaign/20260909-M0-PVA`, actual
  worker16412 (parent17516). It is WAITING for this fit, then checks both NN
  plateau results and finite five-flight coupled diagnostics, prepares ONE
  fresh PPO with its frozen ppo_settings.json (1024 envs, flown start/target),
  runs that worker, generates best-policy rehearsal and a portable ZIP. It
  stops for review on fit/training/export failures or safety ceilings; no
  automatic restarts. DO NOT independently start another PPO while this
  campaign is waiting/running. `training_job.json` will identify its actual
  job once started. To cancel future work, touch campaign/STOP; if training is
  running, it forwards STOP to that new training job. PhysX/old PPO untouched.
- Follow training to meaningful stopping, inspect best policy/rehearsal and
  save representative outputs. Do not claim simulated success as real flight.
- Optional bounded meaningful MPPI run on the completed model; the independent
  runner/export must be tested, but no need to promise optimization success.
- Update HANDOFF/progress with exact final paths, limits and evidence. Pause
  heartbeat when all work is done.

Hourly thread heartbeat `finish-pva-research-workflow` is ACTIVE, destination
thread. Two earlier create attempts failed for missing destination and created
nothing. This is the only new heartbeat. Inspect workers before any launch.
No active production PVA PPO exists yet; the campaign will launch it after fit.
Full-whip NN update3 selection0.50955 improved from0.52357, finite gradients.
At ~04:30 local update6 is running (last completed update5 loss0.50399).
Physical-fit audit nuance: its 1.02 s rollout starts at the last pre-onset
sample, ending 1.0109–1.0192 s after onset depending on the take. It therefore
includes 1–2 early return samples. The active cable NN objective is explicitly
masked to 0–1 s, and combined whip RMS is also restricted to the whip. Do not
describe the physical coefficient objective as an exactly 0–1 s mask.
PhysX stays paused. Original assets
and raw/normalized source data are preserved.
