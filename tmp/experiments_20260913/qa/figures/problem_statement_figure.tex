% Requires in the main preamble:
% \usepackage{tikz}
% \usepackage{subcaption}
% \input{figures/aerowhip_tikz_styles.tex}

\begin{figure*}[t]
  \centering
  \begin{subfigure}[t]{0.495\textwidth}
    \centering
    \resizebox{\linewidth}{!}{%
      \input{figures/problem_whipping_motion.tex}%
    }
    \caption{Whipping motion.}
    \label{fig:problem_whipping}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.495\textwidth}
    \centering
    \resizebox{\linewidth}{!}{%
      \input{figures/problem_dder_model.tex}%
    }
    \caption{Reduced DDER representation.}
    \label{fig:problem_dder}
  \end{subfigure}

  \caption{Aerial cable whipping problem and cable representation. (a) Attachment motion forms a traveling fold that propagates toward the free end and unfolds as the tip extends toward the target. Open markers indicate fixed material samples along the cable. (b) The cable is represented by material nodes connected by fixed-length segments. The root follows the quadrotor attachment, the distal node is free, and local segment tangents define the discrete bending geometry.}
  \label{fig:problem_statement}
\end{figure*}
