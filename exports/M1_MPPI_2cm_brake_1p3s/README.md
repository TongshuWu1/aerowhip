# M1 flight CSV

Use `fullstate_30hz.csv` with the same controller and launch method. Keep your four-whip recording routine with 15-second settling between repetitions.

Execute the complete CSV, including the 1.3-second brake, return, and final hold. Launch tracked origin: (0, 0, 1.4) m. CSV duration: 8.400 s.

Save the OptiTrack and controller logs in `flight_take/M1/`. No physical target is assumed for this collection.

M1 was fitted from all 11 M0 whips, keeping individual missing-marker masks. Training stopped at the user's request and retained cable checkpoint 270. The CSV uses M1 to follow the same frozen M0 reference motion. New-flight prediction performance remains to be measured.
