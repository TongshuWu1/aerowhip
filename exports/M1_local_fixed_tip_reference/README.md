# M1 corrected command for the next physical experiment

Use `fullstate_30hz.csv` with the existing 30 Hz FullState playback program.

- Tracked-origin launch: **(0, 0, 1.4) m**.
- Target: **(1.25, 0, 1.25) m**.
- Complete duration: **7.20 s**, 217 timestamped rows.
- Execute all rows: whip, 1.00 s smooth brake, 2.0667 s return, 3.00 s final hold.
- Establish the same settled launch hover and hanging cable before playback.
- CSV fields retain the existing position/velocity/kinematic-acceleration and zero-yaw convention.

M1 fitting is unchanged. Only the original M0 B-spline command coefficients
were corrected. The desired tip and quadrotor trajectories and their timing
are the original M0 prediction. No direct measured-trajectory bias was added,
and no new MPPI whip was designed.

M1 predicts a tip-reference RMSE of 3.73 cm (unchanged M0 command under M1:
14.41 cm), quadrotor-reference RMSE of 4.43 cm, and maximum quadrotor-reference
deviation of 7.54 cm through 1.13333 s. Predicted target error at the original
strike time, 1.117248 s, is 5.41 cm. These are simulation estimates, not measured
physical performance or a binary hit claim.

Complete nonlinear recovery replay, standard/fast model agreement, exact CSV
serialization, independent B-spline P/V/A reconstruction, continuity at all
recovery joins, model identity, and offscreen Rehearsal loading passed on
Windows / RTX 4080. Physical M1 execution remains to be evaluated; clearance
from the propellers is not certified by these checks.

## Save the new flight data

Put paired raw recordings in `flight_take/`, using the existing naming:
`M1_001.csv` for OptiTrack and `experiment_M1_001.csv` for the controller log,
then increment the take number. Preserve every raw take and this exact CSV.
Record any intervention or abnormal execution so its usable interval can be
reviewed. No real-flight measurements have been generated by this tool.

The accompanying `reference.npz` preserves the original M0 desired physical
motion for the later comparison. Measure time-matched tip and quadrotor RMSE
on [0, 1.13333] s, target error at 1.117248 s, and closest target error over
[0, 1.5] s. Do not shift the motion in time or position to improve the metrics.

The application startup replay selects this saved correction. In an already
open app, refresh Rehearsal and open the entry named
`20260913-032256-816506-M1-local-fixed-tip-reference`.
